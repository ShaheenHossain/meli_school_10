<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="p_task.list">
      <field name="name">p_task list</field>
      <field name="model">p_task.p_task</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="p_task.action_window">
      <field name="name">p_task window</field>
      <field name="res_model">p_task.p_task</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="p_task.action_server">
      <field name="name">p_task server</field>
      <field name="model_id" ref="model_p_task_p_task"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="p_task" id="p_task.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="p_task.menu_1" parent="p_task.menu_root"/>
    <menuitem name="Menu 2" id="p_task.menu_2" parent="p_task.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="p_task.menu_1_list" parent="p_task.menu_1"
              action="p_task.action_window"/>
    <menuitem name="Server to list" id="p_task" parent="p_task.menu_2"
              action="p_task.action_server"/>
    -->
  </data>
</odoo>